# 📖 Read the daily text right from your terminal

1. Clone repository
2. `yarn` or `npm install`
3. Create a .env file, add a SITE property which contains the link to the daily text in your language
4. `node index.js`
